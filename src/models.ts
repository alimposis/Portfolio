export type typeArgumets = FormDataEntryValue | null;

export interface IActionState {
    data: null | string;
    error: null | string;
}
export interface IListStack {
    name: string;
    description: string;
    progress: string;
    time: string;
    id: string;
}
